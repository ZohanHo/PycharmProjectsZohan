test = 10

print("Мне" str(test) + " лет"+"!")    #получим ошибку если не пометим что переменная  test - строка,
                                       #  потому что нельзя соеденять строки и переменную типа не строк .